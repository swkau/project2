<p id="notice"><%= notice %></p>

<h1>Listing Accounts</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Industry</th>
      <th>Relationship type</th>
      <th>Company size</th>
      <th>Company email</th>
      <th>Company phone</th>
      <th>Company fax</th>
      <th>Company address1</th>
      <th>Company address2</th>
      <th>Company state</th>
      <th>Company country</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td><%= account.name %></td>
        <td><%= account.desc %></td>
        <td><%= account.industry %></td>
        <td><%= account.relationship_type %></td>
        <td><%= account.company_size %></td>
        <td><%= account.company_email %></td>
        <td><%= account.company_phone %></td>
        <td><%= account.company_fax %></td>
        <td><%= account.company_address1 %></td>
        <td><%= account.company_address2 %></td>
        <td><%= account.company_state %></td>
        <td><%= account.company_country %></td>
        <td><%= account.status %></td>
        <td><%= link_to 'Show', account %></td>
        <td><%= link_to 'Edit', edit_account_path(account) %></td>
        <td><%= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Account', new_account_path %>
